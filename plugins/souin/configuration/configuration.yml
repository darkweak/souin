default_cache:
  port:
    web: 80
  regex:
    exclude: /excluded
  ttl: 5s
cache_keys:
  .+:
    disable_body: true
    disable_url: false
    disable_query: true
log_level: debug
reverse_proxy_url: 'http://traefik'
