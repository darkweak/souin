version: '3.4'

x-networks: &networks
  networks:
    - your_network

services:
  souin:
    ports:
      - 80:80
#      - 443:443
    depends_on:
      - redis
    volumes:
      - ./traefik.json:/ssl/traefik.json
    <<: *networks

  redis:
    image: redis:alpine
    <<: *networks

networks:
  your_network:
    external: true
